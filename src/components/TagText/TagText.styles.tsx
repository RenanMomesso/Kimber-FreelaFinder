import { StyleSheet } from 'react-native'

export default (color: any) => StyleSheet.create({
    container: {
        backgroundColor: color,
        borderRadius: 16,
        paddingHorizontal: 16,
        paddingVertical: 4,
        marginRight: 6,
        marginBottom:6,
       
    }
})