<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@drawable/background_splash</item>
        <item name="android:colorControlActivated">#016DF7</item>
        <item name="android:textCursorDrawable">@drawable/editext_cursor</item>
        <item name="android:colorControlHighlight">#016DF7</item>
        <item name="android:colorControlNormal">#6B6B6B</item>
    </style>

</resources>
